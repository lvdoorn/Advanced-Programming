-module(flamingo).
-export([ new/1
		, route/4
		, request/4
		]).

%% API

new(Global) -> todo.

route(Flamingo, Prefixes, Action, Arg) -> todo.

request(Flamingo, Request, From, Ref) -> todo.


%% Internal

